extends layout

block content
  h1= title
  a(href="/new") Send a message
  ul
    each message in messages
      li
        span !{message.text}
        | : 
        span !{message.user}
        | : 
        span !{message.added}

    else
      li There are no messages.